import { Box } from "@mui/material";
import React from "react";

const Lists = () => {
  return (
    <Box
      sx={{
        minHeight: 790,
        bgcolor: "#fff",
        borderRadius: "10px",
        padding: "10px",
        display: "flex",
        flexDirection: "column",
        gap: "20px",
      }}
    >
      Lists
    </Box>
  );
};

export default Lists;
